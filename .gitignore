# --- Dependencies ---
node_modules/

# --- Build output ---
dist/
out/
release/
*.tsbuildinfo

# --- IDE / OS noise ---
.DS_Store
.eslintcache
*.log*

# --- Local env / secrets ---
.env
.env.*
!.env.example

# --- Local databases / fixtures ---
*.sqlite
archivio_test/
electron-starter-003/

# --- Test outputs ---
scripts/.e2e-out/

# --- Custom extension drop-in (do NOT publish) ---
# Optional folder for a private extension repo checked out alongside the host.
# Loaded by the Vite aliases when LOAD_EXTENSION=1 is set; without the
# directory the aliases fall back to the public stubs in src/extension/.
/extension/
/build-siever.mjs
